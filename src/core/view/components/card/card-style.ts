import { StyleSheet } from "react-native";

const cardStyle = StyleSheet.create({
  view:{
    width: '100%',
    padding: 10,
    borderWidth: 1,
    borderRadius: 10,
  }
})

export default cardStyle