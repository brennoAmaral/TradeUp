import { StyleSheet } from "react-native";

const usersDetailStyle = StyleSheet.create({
  containerImage:{
    display:'flex',
    flexDirection:'row',
    justifyContent:'center',
    alignItems:'center',
    paddingVertical:25
  },
  image: {
    width: 200,
    height: 200,
    borderRadius: 500,
  },
});

export default usersDetailStyle
